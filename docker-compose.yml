services:
  mongodb:
    image: mongo:8.0.17
    container_name: horrorz_db
    ports:
      - "27017:27017"
    environment:
      - TZ=America/Santiago
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data: